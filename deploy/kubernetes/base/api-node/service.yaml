apiVersion: v1
kind: Service
metadata:
  name: api-node
  labels:
    app: api-node
spec:
  type: ClusterIP
  selector:
    app: api-node
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  sessionAffinity: None
