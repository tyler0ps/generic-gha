apiVersion: v1
kind: Service
metadata:
  name: client-react
  labels:
    app: client-react
spec:
  type: ClusterIP
  selector:
    app: client-react
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
