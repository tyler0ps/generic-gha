apiVersion: v1
kind: Service
metadata:
  name: api-golang
  labels:
    app: api-golang
spec:
  type: ClusterIP
  selector:
    app: api-golang
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
